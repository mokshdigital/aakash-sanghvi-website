/** @type {import('next').NextConfig} */
const nextConfig = {
    // Ensure trailing slashes are handled consistently if that's an issue (optional)
    // trailingSlash: false, 
};

export default nextConfig;
